import React from 'react';
import { Typography } from '@mui/material';

const RouteError = () => {
  
  return (
    <div>
      <Typography>Error esta ruta no Existe : (</Typography>
    </div>
  );
};

export default RouteError;
